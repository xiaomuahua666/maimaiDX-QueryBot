# 更新日志

## 未发布

- 猜 Rating 新增 1～4 级难度：高难度逐步减少卡片与成绩提示并小幅提高积分/BREAK；题主不能作答或进入结算。
- 新增「B50 找内鬼」群游戏：从 5 张 B50 卡片中找出单曲 RA 被篡改的一张，按最终正确答案的提交速度结算。
- 「我的猜歌」扩展为七玩法并改用自适应卡片布局，补齐找内鬼配色、趋势、占比与近期明细渲染。
- `锐评一下` 改为现有 Token 基础价的 3 倍计费，调用模型前预扣 6 BREAK，成功后多退少补、生成或制图失败全额退回。
- 锐评同段依据补充脱敏聚合样本人数、谱面覆盖率与置信度；推分候选按 B35/B15 正确地板核算预计收益，并移除随机候选抽样，使评价更完整、可执行且可复核。
- 完成表生成时记录曲目与定数签名；曲库数据变化后拒绝继续使用过期底图，并在启动或查询时提示管理员重新生成。
- 图片二维码识别改为带并发与 4 秒超时保护的隔离子进程，避免异常群图或原生解码器长期占用 GIL 导致 Bot 停止收取消息。
- 新增「本群猜歌排行」命令，生成带头像的本群猜歌积分排行榜图片。
- 图片二维码扫描增加多路兜底：原图 → 放大 → 灰度 autocontrast → 锐化，配合 LocalAverage/GlobalHistogram/FixedThreshold 三种 Binarizer，对手机拍屏、群图压缩、低对比图片识别率明显提升。
- 修复开字母玩法中三字符错字容忍导致 `the` 误开曲名、别名均不含 `the` 的歌曲；普通猜歌的原有容错不受影响。
- `舞萌状态` / `mais` 失败率改用 AWMC 全局统计接口，按业务错误、服务端/转发错误、客户端 4xx 分类绘图，自动省略空时段与全空分类。
- 直接发送二维码时即时提示撤回状态与动态预计耗时；未绑定自动绑定，并按已绑定的水鱼/落雪渠道自动上传。
- 明确 `maiu` 仅水鱼、`maiul` 仅落雪、`maiua` 两边；直接二维码默认采用自动渠道模式。
- 群 `669800745` 签到总奖励双倍；群 `993795066` 与 `1072033605` 同享 +25% 加成。
- 发票价格调整为倍率 × 10 BREAK，旧默认倍率单价 2 或 3 会在启动时迁移为 10。
- `我的AWMC` 改为分段聊天记录，并加入账号功能今日、累计、成功/失败及最近流水统计。
- 60 秒内真实功能请求超过 30 个后，第 31 个起由触发者额外支付 1 BREAK；阈值、窗口和价格均可通过 ENV 调整。

## v26.8.1（2026-07-15）

### 新增

- `maibindfish` / `mai绑定水鱼` 支持无参数交互式绑定。
- Bot 会提供水鱼查分器入口，并引导用户在“编辑个人资料”中生成和复制
  `Import-Token`。
- Token 格式错误时继续等待，最多允许输入 3 次；支持发送“取消”退出。
- `今日舞萌` 根据每日人品值发放 BREAK：四舍五入至十位后除以 10，
  每天只可领取一次。
- 自动识别消息图片中的二维码；有效舞萌二维码会进入账号更新流程，普通图片静默忽略。
- 直接发送 SGWCMAID、官方链接或二维码图片时，无账号记录会自动验真绑定/认领。

### 优化

- 用户提交的水鱼 Token 会优先撤回；撤回失败时明确提醒用户手动撤回。
- 绑定成功只显示脱敏 Token，并附带可供管理员追踪的 `REF_ID`。
- 水鱼绑定提示、重试与结果引用触发消息，减少多人同时操作时的歧义。
- 连续签到恢复原奖励曲线，第 5 天以后继续线性增长，不再设置奖励上限。
- 直发舞萌凭据会在绑定后同步 PC，并按水鱼 Token / 落雪 OAuth 状态自动上传；
  PC 服务未配置时仍可完成绑定与上传。
- 机台操作改为全局串行：PC 同步完成后等待 3 秒，再依次执行水鱼与落雪上传；
  不同用户共用同一 keychip 时也不会并发登录互相挤掉。
- 落雪 OAuth 上传直接读取机台全量成绩；Access Token 被拒绝时会强制刷新并重试一次，
  刷新响应未轮换 Refresh Token 时仍可继续使用旧 Token。
- 兼容落雪 OAuth Token 接口的新标准顶层响应与旧版 `success/data` 包装；
  上传失败时会区分 PC 读取、Token 失效及 `write_player`/隐私写入权限问题。
- 同一用户的 OAuth 刷新改为串行并在加锁后重新读取 Token，避免 Refresh Token
  发生轮换时被并发请求互相作废。
- 发票默认仅允许 2、3、5 倍，并支持通过
  `AWMC_TICKET_ALLOWED_MULTIPLIERS=2,3,5` 配置倍率白名单。

### 修复

- 修复发票成功后被错误报告为 `FinishedException()` 的问题。
- 一并修复账号、地图、队列、落雪 B50 等流程中同类的成功结果误报。
- 修复 `mymai` 丢失响应外层 `BanState` 而显示“封禁状态：未知”的问题。
- 修复 team 模式票券查询误传 `userId` 导致 `returnCode=102` / 无法获取；
  现已按 maibot 使用 `qrcode + keychip`，并兼容 `userCharge` 包装与免费票券。

> 从旧版本升级不会修改或清空已有账号、BREAK、签到、Token 或成绩数据。
