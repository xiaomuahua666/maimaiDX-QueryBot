# maimaiDX 指令帮助

> 本文档仅收录当前 Bot 已实现的普通用户指令，不含管理员指令与高级功能指令。

---

## 基础

| 指令 | 说明 |
|------|------|
| `帮助maimaiDX` | 查看帮助图 |
| `公告列表` | 查看最近 10 条公告；普通用户不显示内部公告 ID |
| `确认阅读公告` | 确认当前必读公告，确认同一版本后不再重复提示 |
| `项目地址maimaiDX` | 查看项目信息 |
| `今日mai` / `今日舞萌` | 今日运势、推荐曲目；人品值四舍五入后 ÷10 领取每日 BREAK |
| `主题` | 查看或切换成绩图主题 |
| `查看排名` | 查看全服 Rating 排行 |
| `我的排名` | 查看自己的 Rating 排名 |

---

## 曲目查询

| 指令 | 说明 |
|------|------|
| `查歌 <关键词>` | 按曲名 / 别名搜索 |
| `定数查歌 <定数>` | 按定数搜索 |
| `bpm查歌 <bpm>` | 按 BPM 搜索 |
| `曲师查歌 <曲师>` | 按曲师搜索 |
| `谱师查歌 <谱师>` | 按谱师搜索 |
| `<别名>是什么歌` | 通过别名查曲目 |
| `id <歌曲ID>` | 查看歌曲详情 |
| `谱面<ID><难度>` | 谱面预览，难度：绿 / 黄 / 红 / 紫 / 白 |
| `提取曲绘 <歌曲ID>` | 提取曲绘图片 |
| `mai什么` | 随机推荐一首曲目 |
| `mai什么推分` | 随机推荐适合上分的曲目 |
| `来个<等级>` | 随机推荐指定等级曲目 |
| `来个dx<等级>` / `来个sd<等级>` | 随机 DX / 标准曲 |
| `来个<难度><等级>` | 随机指定难度与等级，如 `来个紫14+` |

> 可直接发送 `SGWCMAID`、舞萌官方二维码链接或二维码图片；Bot 会自动撤回、绑定/更新账号、同步 PC，并按绑定状态上传水鱼/落雪，同时提示动态预计耗时。

---

## 别名

| 指令 | 说明 |
|------|------|
| `<曲名>有什么别名` | 查看曲目别名 |
| `id<歌曲ID>有什么别名` | 按 ID 查看别名 |
| `添加别名 <ID> <别名>` | 申请为曲目添加别名 |
| `添加本地别名 <ID> <别名>` | 添加仅本 Bot 生效的本地别名（群主/群管/Bot 管理） |
| `同意别名 <Tag>` | 对别名申请投票 |
| `当前投票` | 查看进行中的别名投票 |

---

## 成绩查询

| 指令 | 说明 |
|------|------|
| `b50` | 查询 B50 成绩图 |
| `ab50` | 查询全部最佳 50 首 |
| `刷新b50` | 强制刷新成绩缓存 |
| `合作b50` | 两人合作 B50 对比 |
| `合作a50` | 两人合作全部最佳 50 对比 |
| `我有多菜` | Rating 对比图 |
| `我在群里有多菜` | 群内 Rating 对比 |
| `minfo` | 查询个人游玩信息 |
| `ginfo` | 查询群组游玩信息 |
| `分数线` | 查询分数线 |
| `友人对战` | 群内随机友人对战（可加数字收紧 Rating 差，如 `友人对战 300`） |

### 群单曲排名

| 指令 | 说明 |
|------|------|
| `<曲名>排名` | 群内该曲成绩排名 |
| `我的<曲名>排名` | 自己在群内的该曲排名 |
| `<曲名>排名 <N>` | 查看前 N 名 |

支持在曲名前加难度，如 `白潘排名`、`我的白潘排名`。

---

## 定数表 / 完成表 / 进度

| 指令 | 说明 |
|------|------|
| `<等级>定数表` | 等级定数表，如 `13+定数表` |
| `<等级>完成表` | 等级完成表，如 `13+ap完成表` |
| `<牌子>完成表` | 牌子完成表，如 `晓极完成表` |
| `<牌子>进度` | 牌子进度，如 `晓极进度` |
| `<等级><达成>进度` | 等级进度，如 `13+sss进度` |
| `<等级><牌子>进度` | 等级牌子进度，如 `13将进度`、`14+极进度` |
| `我要上<分数>分` | 吃分推荐 |
| `我要在<等级>上<分数>分` | 指定等级的吃分推荐 |
| `今日吃分推荐` | 根据存档生成图片版个性化推分清单 |
| `<等级>分数列表` | 该等级分数列表 |

达成标记支持：`ap` `fc` `fcp` `fs` `fsp` `ss` `sss` `sync` 等。

---

## 猜歌

| 指令 | 说明 |
|------|------|
| `猜歌` | 文字猜歌 |
| `猜曲绘` / `猜曲绘1`～`猜曲绘4` | 曲绘猜歌，可指定难度 |
| `猜曲子` | 音频猜歌 |
| `猜铺面` / `猜谱面` | 谱面视频猜歌（无 BGM/无背景，带正解音与倒计时） |
| `舞萌开字母` / `开字母` | 多曲标题开字母看板；局内不计分，通关后按自适应用时星级与贡献结算；`不玩了` 揭晓不发奖 |
| `开字母排行` / `开字母积分榜` | 本群开字母结算积分榜（图） |
| `开字母贡献榜` | 本群开字母贡献权重榜（图） |
| `开字母时间榜` | 本群最佳通关用时榜（图，含头像） |
| `查加倍卡` | 查看自己的加倍卡 |
| `猜歌积分排行` | 总积分排行 |
| `猜歌积分日榜` / `周榜` / `月榜` / `年榜` / `赛季榜` | 各周期积分榜 |
| `猜歌历史日榜` / `周榜` / `月榜` / `年榜` / `赛季榜` | 历史周期榜 |
| `我的猜歌` / `猜歌数据` / `猜歌统计` | 个人五模式（猜歌/曲绘/曲子/铺面/开字母）趋势、占比扇形图与近期明细；可 `@某人` 查对方 |

---

## PC 数

| 指令 | 说明 |
|------|------|
| `更新pc数` | 绑定机台二维码并同步 PC 数 |
| `我的pc数` | 查看个人 PC 数统计 |
| `pc排行` | 全部用户 PC 数排行榜 |
| `pc数 <歌曲>` | 查看指定曲目 PC 数 |
| `pc50` | PC 数 B50 |
| `pca50` | B50 内按 PC 排序 |
| `游玩排行50` | 游玩最多的 50 首谱面 |

---

## AWMC 账号与上传

| 指令 | 说明 |
|------|------|
| `mai账号` | 查看合并后的账号功能帮助 |
| `mai绑定` / `maibind` | 绑定并验证舞萌账号；可认领同 UID 旧记录 |
| `mai解绑` / `mai状态` / `mymai` | 解绑 / 查看详细状态；缓存失效时刷新二维码 |
| `mai绑定水鱼 <Token>` / `maibindfish <Token>` | 绑定水鱼上传 Token |
| `lxbind` | 绑定落雪 OAuth，上传无需导入 Token（推荐） |
| `mai绑定落雪 <导入Token>` / `maibindlx <导入Token>` | 绑定落雪导入 Token（兼容） |
| `maiu` / `导` | 仅上传水鱼 |
| `maiul` | 仅上传落雪 |
| `maiua` | 同时上传水鱼与落雪 |
| `发票` / `fp <2-6>` / `mai查票` | 发放 / 查询票券 |
| `mai地图` / `maiping` | 查询游玩地区 / 测试 API |
| `用户协议` | 查看协议链接并按网页确认词完成同意 |
| `不同意共享我的数据` | 关闭脱敏成绩/Rating/趋势的公开共享（默认开启） |
| `同意共享我的数据` | 重新开启数据共享 |
| `数据共享状态` | 查看当前是否共享脱敏数据 |
| `迁移Koishi 检查/确认 <数据库>` | 超级管理员预检/导入 Koishi maiBot 数据 |
| `存储状态` / `存储同步` | 插件管理员检查或立即同步统一存储 |
| `存储迁移 检查/确认 <源> <目标>` | 在 sqlite / yaml / mysql 间迁移 |

绑定账号后，`更新pc数` 会直接使用已保存二维码。`maiul` / `maiua` 优先复用
`lxbind` OAuth 上传；没有 OAuth 时才使用 `mai绑定落雪` 的兼容导入 Token。
直接发送 `SGWCMAID`、舞萌官方二维码链接或含二维码的图片时，Bot 会自动识别并优先
撤回原消息；没有账号记录时自动绑定，随后同步 PC，并按水鱼 Token / 落雪 OAuth
（或兼容 Token）状态自动上传。

---

## 落雪查分器

| 指令 | 说明 |
|------|------|
| `lxbind` | 绑定落雪查分器（OAuth） |
| `lxunbind` | 解绑落雪查分器 |
| `lxb50` | 使用落雪数据源查询 B50 |
| `数据源` | 查看当前数据源 |
| `数据源 水鱼` / `数据源 落雪` | 切换个人数据源 |

---

## 谱面印象

| 指令 | 说明 |
|------|------|
| `谱面印象 <曲目>` | 查看谱面印象 |
| `写谱面印象 <曲目> <内容>` | 上传谱面印象 |
| `回复谱面印象 <印象ID> <内容>` | 回复某条印象 |
| `点赞谱面印象 <印象ID>` | 为印象点赞 |

---

## AWMC BREAK

| 指令 | 说明 |
|------|------|
| `AWMC签到` | 每日签到获取 BREAK；连续签到奖励不设上限 |
| `AWMC补签` / `补签` | 补昨天；每月最多 3 次，依次消耗 30/60/90 BREAK；不补发奖励 |
| `我的AWMC` | 以分段聊天记录查看 BREAK、账号功能统计和最近流水 |
| `AWMC帮助` | 查看 BREAK 系统说明 |
| `转账BREAK @用户 数量` | 向其他用户转账 |
| `BREAK抽奖 [1-10]` | 默认每次消耗 2 BREAK |
| `BREAK抽奖 帮助` | 查看奖池、概率、消耗和示例 |

> 查分指令每日首次实际请求查分器 API 免费，之后每次扣 1 BREAK（缓存命中不扣）。
> 猜歌每次猜对默认获得 1 BREAK，不设每日上限，积分倍率不放大 BREAK。
> 成绩上传每日首次成功免费；之后默认水鱼 2、落雪 2、同时上传 3 BREAK。
> 发票每次成功按倍率 × 10 BREAK 扣费，例如 2 倍 20、3 倍 30、5 倍 50；失败不扣费。
> 60 秒内真实功能请求超过 30 个后，额外请求的触发者每次加收 1 BREAK。

---

## 管理员审计

| 指令 | 说明 |
|------|------|
| `查询REF <REF_ID>` | 查看脱敏后的完整处理链路 |
| `封禁用户 @用户 [小时] [原因]` | 禁止用户调用本插件，0 小时为永久 |
| `解封用户 @用户` / `封禁列表` | 解除或查看封禁 |
| `管理面板` | 获取已配置的 WebUI 地址 |
| `发布公告 [必读\|不必读] <内容>` | 创建公告并设为唯一当前公告，默认不必读 |
| `编辑公告 <ID> [必读\|不必读] [新内容]` | 编辑公告；编辑当前公告会生成需重新阅读的新版本 |
| `删除公告 <ID>` | 删除公告；删除当前公告后不会回退启用历史公告 |
